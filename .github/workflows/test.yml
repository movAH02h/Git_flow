name: check_program

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  check:

    runs-on: ubuntu-latest

    steps:
    - name: Tests
      run: |
        pytest ./tests/test_add_sub_mult_div.py
